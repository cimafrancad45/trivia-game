//variables for the game
var correct = 0;
var wrong = 0;
var unanswered = 0;
var timer = 30;
var transitionTimer = 5;

// countdown function
function countdown(){
    timer(decrement, 1000);
};

function resetClock(
    timer = 30
);

